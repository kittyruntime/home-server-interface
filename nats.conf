# Brume NATS configuration
# Change the passwords below before deploying to production.

max_payload: 67108864  # 64 MB

jetstream {
  store_dir: "/data"
}

authorization {
  users: [
    {
      user: "backend"
      password: "brume-backend-dev"
      permissions: {
        publish:   [ "brume.root.>", "_INBOX.>", "$JS.>" ]
        subscribe: [ "brume.events.>", "_INBOX.>", "$JS.>" ]
      }
    }
    {
      user: "worker"
      password: "brume-worker-dev"
      permissions: {
        publish:   [ "brume.events.>", "_INBOX.>", "$JS.>" ]
        subscribe: [ "brume.root.>", "_INBOX.>", "$JS.>" ]
      }
    }
  ]
}
